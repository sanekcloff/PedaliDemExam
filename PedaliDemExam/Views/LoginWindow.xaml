<Window x:Class="PedaliDemExam.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PedaliDemExam.Views"
        xmlns:vm="clr-namespace:PedaliDemExam.ViewModels"
        mc:Ignorable="d"
        Title="Окно входа" Height="450" Width="300" WindowStartupLocation="CenterScreen">
    <Window.DataContext>
        <vm:LoginWindowViewModel/>
    </Window.DataContext>
    
    <Grid>

        <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center" Width="200">
            <TextBox Text="{Binding Login}"/>
            <TextBox Text="{Binding Password}"/>
            <Button Content="LOGIN" Command="{Binding LoginCommand}"/>
        </StackPanel>
        
    </Grid>
    
</Window>
